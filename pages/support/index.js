import styled from 'styled-components'
import Container from '../../components/Container'

const SupportStyled = styled(Container).attrs({
  className: '',
})``

const Support = () => <SupportStyled>Support</SupportStyled>

export default Support
